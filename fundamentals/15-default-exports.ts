export const COURSE_TOTAL = 20;

export const TYPESCRIPT_COURSE = {
    title: "hello",
    subtitle: "sub",
    lessonCount: 10
};

//Exporta una única cosa principal
export default function printCourse(course) {
    console.log(`the course title is ${course}`);
}