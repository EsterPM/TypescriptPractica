export type Course = {
    readonly title:string,
    subtitle:string,
    lessonsCount?:number
};

export const PAGE_SIZE = 100;
const pageSize = PAGE_SIZE;

export const COURSE = {
    title: "Hello",
    subtitle: "sub",
    lessonsCount: 10
};